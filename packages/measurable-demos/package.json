{
  "name": "@matthuggins/measurable-demos",
  "version": "1.0.0",
  "description": "Interactive demos of the measurable unit-conversion library, embedded in a blog post",
  "private": true,
  "type": "module",
  "main": "src/main.ts",
  "types": "src/main.ts",
  "exports": {
    ".": "./src/main.ts"
  },
  "scripts": {
    "build": "tsc -b",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format .",
    "format:fix": "biome format --write .",
    "typecheck": "tsc --noEmit",
    "test": "vitest --run --passWithNoTests",
    "test:watch": "vitest --watch",
    "test:lint": "pnpm run lint && pnpm run format && pnpm run typecheck && pnpm run test"
  },
  "keywords": [],
  "author": "Matt Huggins",
  "license": "UNLICENSED",
  "dependencies": {
    "@matthuggins/ui": "workspace:*",
    "@phosphor-icons/react": "catalog:",
    "measurable": "^3.1.0",
    "remeda": "^2.39.0"
  },
  "devDependencies": {
    "@matthuggins/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  },
  "peerDependencies": {
    "react": "catalog:",
    "react-dom": "catalog:"
  }
}
