{
  "name": "@matthuggins/platforming-engine",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "keywords": [],
  "author": "Matt Huggins",
  "license": "UNLICENSED",
  "type": "module",
  "main": "src/main.ts",
  "types": "src/main.ts",
  "exports": {
    ".": "./src/main.ts"
  },
  "scripts": {
    "build": "tsc -b",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format .",
    "format:fix": "biome format --write .",
    "typecheck": "tsc --noEmit",
    "test": "vitest --run --passWithNoTests",
    "test:watch": "vitest --watch",
    "test:lint": "pnpm run lint && pnpm run format && pnpm run typecheck && pnpm run test"
  },
  "devDependencies": {
    "@matthuggins/tsconfig": "workspace:*",
    "@matthuggins/ui": "workspace:*",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "jsdom": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  },
  "dependencies": {
    "auto-bind": "^5.0.1"
  }
}
