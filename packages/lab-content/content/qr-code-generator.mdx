---
title: QR Code Generator
description: Design and download a styled QR code entirely in the browser, with center icons, frames, and custom colors
published: true
keywords: [qr code, qr code generator, qr, barcode, svg, png, wifi qr code, vcard, react, typescript]
thumbnail: /lab/qr-code-generator.svg
ogImage: /lab/qr-code-generator.og-image.png
schemaType: WebApplication
---

import { QrCodeGenerator } from "../src/lazy/QrCodeGenerator"

Point it at a link, a Wi-Fi network, or a contact card, then style the result:
module and finder shapes, a color palette, an icon in the middle, and a frame
with your own caption. Everything runs in your browser, so nothing you type is
uploaded anywhere, and the download is a plain SVG or PNG.

<QrCodeGenerator className="mt-6" />
